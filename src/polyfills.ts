/**
 * Zone JS is required by default for Angular itself.
 * The zone.js package import path was updated from `zone.js/dist/zone` to `zone.js`
 * as of zone.js 0.13.x. Polyfills are also declared in angular.json; this file
 * is retained for editor tooling compatibility.
 */
import 'zone.js';
